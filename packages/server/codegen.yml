overwrite: true
schema: "src/api/apollo/typedefs/*.graphql"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
